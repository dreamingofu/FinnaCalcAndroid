<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this is only the pre-Compose window theme. -->
    <style name="Theme.FinnaCalc" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
